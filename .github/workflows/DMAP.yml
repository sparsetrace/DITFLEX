name: DMAP

# Manual only. Loads the 7M-step SiT-XL/2 (or resumes/warm-starts per the
# resolution chain in DMAP.py), FOLDS W_Q,W_K -> W_M (== tied W_Q=W_K init,
# W_N and the flux dropped), and either smoke-tests or runs a flow-matching
# finetune of the FOLDED DMAP model on your HF latents, on a Modal GPU.
# Step-0 grids (pre-fold and post-fold, provenance-tagged) are rendered
# unconditionally. The AMAP surgery itself lives in the AMAP workflow.
#
# PROTOCOL: lr is constant 1e-4, matched with the AMAP arm. Change only in
# lockstep across both workflows.
on:
  workflow_dispatch:
    inputs:
      gpu:
        description: "Modal GPU"
        type: choice
        options: [H200, B200, H100, A100-80GB]
        default: H200
      precision:
        description: "matmul precision (tf32 matches the ditflex chain)"
        type: choice
        options: [tf32, highest, bf16]
        default: tf32
      stage:
        description: "smoke (load+fold+grid) or finetune"
        type: choice
        options: [finetune, smoke]
        default: finetune
      steps:
        description: "N steps to train THIS run (added on top of any resumed step)"
        type: string
        default: "500"
      lr:
        description: "finetune LR (PROTOCOL: constant 1e-4, matched with AMAP arm)"
        type: string
        default: "0.0001"
      save_every:
        description: "push a checkpoint every N steps"
        type: string
        default: "10000"
      max_shards:
        description: "latent shards to load (0 = all 32)"
        type: string
        default: "0"
      resume:
        description: "resume from latest checkpoint in push_repo if present"
        type: choice
        options: [auto, never, must]
        default: auto
      sample_every:
        description: "preview grid every N finetune steps (0 = only start+end)"
        type: string
        default: "10000"
      sample_steps:
        description: "ODE sampling steps"
        type: string
        default: "50"
      cfg_scale:
        description: "classifier-free guidance scale"
        type: string
        default: "4.0"
      latents_repo:
        description: "your HF latents dataset (required for finetune)"
        type: string
        default: "sparsetrace/dlatentzz"
      push_repo:
        description: "HF repo to push checkpoints/samples to (must be writable)"
        type: string
        default: "jcandane/DMAP"
      amap_repo:
        description: "AMAP repo to warm-start+fold from when no DMAP checkpoint exists"
        type: string
        default: "jcandane/AMAP"
      qk_rmsnorm:
        description: "per-head RMSNorm on mu (stabiliser, off by default)"
        type: boolean
        default: false
      learn_logit_scale:
        description: "per-head learnable logit scale (stabiliser; freeze during matched transfer)"
        type: boolean
        default: false

permissions:
  contents: write   # commit sample grids back into the repo

jobs:
  dmap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install modal
      - name: modal run DMAP
        working-directory: DMAP
        env:
          MODAL_TOKEN_ID: ${{ secrets.MODAL_TOKEN_ID }}
          MODAL_TOKEN_SECRET: ${{ secrets.MODAL_TOKEN_SECRET }}
          DMAP_GPU: ${{ inputs.gpu }}
        run: |
          modal run DMAP.py \
            --stage "${{ inputs.stage }}" \
            --steps "${{ inputs.steps }}" \
            --lr "${{ inputs.lr }}" \
            --precision "${{ inputs.precision }}" \
            --sample-every "${{ inputs.sample_every }}" \
            --sample-steps "${{ inputs.sample_steps }}" \
            --cfg-scale "${{ inputs.cfg_scale }}" \
            --save-every "${{ inputs.save_every }}" \
            --max-shards "${{ inputs.max_shards }}" \
            --resume "${{ inputs.resume }}" \
            --latents-repo "${{ inputs.latents_repo }}" \
            --push-repo "${{ inputs.push_repo }}" \
            --amap-repo "${{ inputs.amap_repo }}" \
            ${{ inputs.qk_rmsnorm == 'true' && '--qk-rmsnorm' || '--no-qk-rmsnorm' }} \
            ${{ inputs.learn_logit_scale == 'true' && '--learn-logit-scale' || '--no-learn-logit-scale' }}
      - name: Commit sample grids
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add DMAP/samples/*.png || true
          git diff --cached --quiet && echo "no new grids" && exit 0
          git commit -m "DMAP: sample grids [skip ci]"
          git push
